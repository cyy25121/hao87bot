# 注意：在 Cloud Functions v2 中，我們使用 Firebase Secrets Manager
# 這些變數不應該在這裡設定，因為會與 secrets 衝突
# 
# Telegram Bot Token 和 OpenAI API Key 應該透過以下方式設定：
# firebase functions:secrets:set TELEGRAM_BOT_TOKEN
# firebase functions:secrets:set OPENAI_API_KEY
#
# 本地開發時，如果需要使用 .env，請使用不同的變數名稱
# 例如：LOCAL_TELEGRAM_BOT_TOKEN（僅用於本地測試）
